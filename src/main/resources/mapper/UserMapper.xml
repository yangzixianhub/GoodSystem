<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.wanheng.springjwtlogin.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="dev.wanheng.springjwtlogin.domain.User">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="phone" column="phoneNumber" jdbcType="VARCHAR"/>
            <result property="createdAt" column="createdTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,
        phoneNumber,
        createdTime
    </sql>
    <select id="findOneByUsername" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user
        where
        username = #{username,jdbcType=VARCHAR}
    </select>

    <insert id="insert" parameterType="dev.wanheng.springjwtlogin.domain.User">
        INSERT INTO user (username, password, phoneNumber)
        VALUES (#{username}, #{password}, #{phone})
    </insert>
</mapper>
